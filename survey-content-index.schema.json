{
  "$id": "survey-content-index@0.1",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Survey Content Index",
  "description": "Specification for survey content: Index as main register of content package.",
  "type": "object",
  "properties": {
    "packageId": {
      "type": "string",
      "description": "Identifyer for the survey content package"
    },
    "packageVersion": {
      "type": "string",
      "description": "Package Version in SemVer format."
    },
    "packageName": {
      "$ref": "#/$defs/language_coded_texts"
    },
    "packageDescription": {
      "$ref": "#/$defs/language_coded_texts"
    },
    "maintainerName": {
      "type": "string",
      "description": "Name of the person or institution responsable for creating and maintaining",
      "examples": [
        "IQB - Institut zur Qualitätsentwicklung im Bildungswesen"
      ]
    },
    "maintainerUrl": {
      "type": "string",
      "description": "Url of maintainer to get more information or to get in contact",
      "examples": [
        "https://www.iqb.hu-berlin.de/tba"
      ]
    },
    "status": {
      "type": "string",
      "description": "Describes the progress of development and visibility issues. During development, version suffixes may be used like 'beta', 'release candidate' etc.",
      "enum": [
        "IN_DEVELOPMENT",
        "DISCONTINUED",
        "RELEASED_PUBLIC",
        "RELEASED_CONFIDENTIAL"
      ],
      "default": "IN_DEVELOPMENT"
    },
    "metadata": {
      "$ref": "#/$defs/metadata"
    }
  },
  "required": [
    "packageId",
    "packageVersion",
    "packageName"
  ],
  "additionalProperties": false,
  "$defs": {
    "language_coded_texts": {
      "type": "array",
      "description": "Used for storing the input of texts. This type applies also in cases when only one language is used.",
      "items": {
        "type": "object",
        "properties": {
          "lang": {
            "type": "string",
            "minLength": 1,
            "description": "ISO-language code",
            "pattern": "^[a-z]{2}$"
          },
          "value": {
            "type": "string"
          }
        },
        "required": [
          "lang",
          "value"
        ],
        "additionalProperties": false
      },
      "minItems": 1
    },
    "metadata": {
      "$ref": "https://w3id.org/iqb/spec/metadata-values/3.0"
    }
  }
}